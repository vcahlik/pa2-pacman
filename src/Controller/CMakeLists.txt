set(SOURCES
        ${SOURCES}
        ${CMAKE_CURRENT_SOURCE_DIR}/GameController.cpp
        PARENT_SCOPE
        )

set(HEADERS
        ${HEADERS}
        ${CMAKE_CURRENT_SOURCE_DIR}/GameController.h
        PARENT_SCOPE
        )
