#include "EscapeNavigator.h"

EscapeNavigator::EscapeNavigator(const MovableObject &client, const GameMap &map)
    : Navigator(client, map)
{

}

const Direction EscapeNavigator::navigate(const MovableObject &object) const
{

}
